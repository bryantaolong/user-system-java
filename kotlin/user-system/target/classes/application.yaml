spring:
  application:
    name: user-system
  profiles:
    active: dev
  servlet:
    multipart:
      enabled: true
      max-request-size: 10MB
      max-file-size: 1MB
  jackson:
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss

# 日志配置（通用）
logging:
  include-application-name: false
  level:
    root: INFO
  file:
    name: logs/user-system.log
  logback:
    rolling-policy:
      max-file-size: 10MB
      max-history: 30

file:
  upload-dir: ./uploads

# MyBatis 配置（通用）
mybatis:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.bryan.system.domain.entity
  configuration:
    map-underscore-to-camel-case: true
    default-enum-type-handler: org.apache.ibatis.type.EnumOrdinalTypeHandler

# JWT 配置
jwt:
  secret-key: BryanTaoLong2025!@#SuperSecretKeyJwtToken987
  expiration-ms: 86400000  # 24小时
  token-prefix: "Bearer "

# 安全配置
security:
  login-fail-limit: 5              # 登录失败次数限额
  login-fail-reset-minutes: 30     # 登录失败计数重置时间（分钟）
  account-lock-duration-minutes: 30 # 账号锁定时间（分钟）

# CORS 配置
cors:
  allowed-origins: "${CORS_ALLOWED_ORIGINS:http://localhost:5173}"