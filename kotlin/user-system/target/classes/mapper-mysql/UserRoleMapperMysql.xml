<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bryan.system.mapper.UserRoleMapper">

    <resultMap id="BaseResultMap" type="com.bryan.system.domain.entity.UserRole">
        <id column="id" property="id"/>
        <result column="role_name" property="roleName"/>
        <result column="is_default" property="isDefault"/>
        <result column="deleted" property="deleted"/>
        <result column="version" property="version"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="created_by" property="createdBy"/>
        <result column="updated_by" property="updatedBy"/>
    </resultMap>

    <sql id="BaseColumns">
        id, role_name, is_default, deleted, version
        created_at, updated_at, created_by, updated_by
    </sql>

    <select id="selectOneByIsDefaultTrue" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM user_role
        WHERE is_default = 1
        AND deleted = 0
        LIMIT 1
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM user_role
        WHERE deleted = 0
    </select>

    <select id="selectByIdList" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM user_role
        WHERE id IN
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
        AND deleted = 0
    </select>

</mapper>
